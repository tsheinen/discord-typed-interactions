[package]
name = "test-harness"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"
discord_typed_interactions_proc_macro = { path = "../discord-typed-interactions-proc-macro" }